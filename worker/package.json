{
  "name": "uptempo-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Uptempo — un solo Worker multi-cliente: asistente web + WhatsApp, mismo cerebro.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "tsx test/agenda.test.ts",
    "tail": "wrangler tail --format pretty"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "tsx": "^4.23.13",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.10"
  }
}
